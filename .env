# API Configuration — this app's backend
VITE_APP_API_URL=http://localhost:8084/

# Account Center — the central SSO server that redirects users here
VITE_ACCOUNT_CENTER_URL=http://localhost:3000

# DEV MODE — When true, bypasses all step validation to easily test the approval flow end-to-end
VITE_DEV_MODE=false
